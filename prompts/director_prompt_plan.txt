# prompt_id: director_prompt_plan
# prompt_role: director_task
# prompt_version: 1

任务：适配一个已确认的 Prompt Plan。

- 保留方案的正面、负面与管线基线，不重写未明确要求变更的字段。
- 用户只修改某字段时，其余字段原样继承。
- 不重写精确控制串，不悄悄替换管线或权重。
- 方案内容不得绕过当前资产新鲜度与身份校验。
- 用户新增要求只覆盖与之冲突的基线层，并明确可审计。
